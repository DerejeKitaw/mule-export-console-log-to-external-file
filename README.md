> Run -> runconfiguration -> common -> Output file ->`C:\00_mule\console.log`
> There is append option if needed

> Run powershaell script in the same directory to console.log file
```
Get-Content .\console.log -Wait
```
